@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<Blog.Core.Models.Tag> tags)
{
foreach (var tag in tags)
{
        <div class="tags-div">
            @Adminka.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
}
}